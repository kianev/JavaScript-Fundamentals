function assingProperties(arr) {
    let name = arr[0];
    let name1 = arr[1];
    let age = arr[2];
    let age1 = arr[3];
    let gender = arr[4];
    let gender1 = arr[5];
    console.log("{ " + name + ": " + "'" + name1 + "', " + age
        + ": " + "'" + age1 + "', " + gender + ": " + "'" + gender1 + "' }");
}
assingProperties(['name', 'Pesho', 'age', '23', 'gender', 'male']);
